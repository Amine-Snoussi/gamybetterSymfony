{% extends 'base-back.html.twig' %}

{% block title %}Actualite{% endblock %}

{% block body %}
    <h1>Actualite</h1>
  <a href="{{ path('app_actualite_index') }}" class="ri-arrow-go-back-fill btn btn-outline-primary"></a>

    <a href="{{ path('app_actualite_edit', {'idActualite': actualite.idActualite}) }}" class=" btn btn-outline-primary bi bi-pencil-square"></a>
    <table class="table">
        <tbody>
            <tr>
                <th>IdActualite</th>
                <td>{{ actualite.idActualite }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ actualite.image }}</td>
            </tr>
            <tr>
                <th>Video</th>
                <td>{{ actualite.video }}</td>
            </tr>
            <tr>
                <th>IdPersonne</th>
                <td>{{ actualite.idPersonne }}</td>
            </tr>
        </tbody>
    </table>

  

    {{ include('actualite/_delete_form.html.twig') }}
{% endblock %}
