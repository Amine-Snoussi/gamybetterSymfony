{% extends 'base-back.html.twig' %}

{% block title %}Game index{% endblock %}

{% block body %}
    <h1>Game index</h1>

    <table class="table">
        <thead>
            <tr>
               
                <th>Image eq1</th>
                <th>Image eq2</th>
                <th>Score</th>
                <th>LienStreaming</th>
                <th>Status</th>
                <th>Gold</th>
                <th>Duree</th>
                <th>Date</th>
                <th>Temps</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for game in games %}
            <tr>
                
                <td><img src="{{ asset('uploads/images/' ~ game.image1 ) }}" height="70" width="70" ></td>
                 <td><img src="{{ asset('uploads/images/' ~ game.image2 ) }}" height="70" width="70" ></td>
                <td>{{ game.score }}</td>
                <td>{{ game.lienStreaming }}</td>
                <td>{{ game.status }}</td>
                <td>{{ game.gold }}</td>
                <td>{{ game.duree? game.duree|date('H:i:s') : '' }}</td>
                <td>{{ game.date ? game.date|date('Y-m-d') : ''  }}</td>
                <td>{{ game.temps? game.temps|date('H:i:s') : '' }}</td>
                <td>
                     <a href="{{ path('app_game_show', {'idMatch': game.idMatch}) }}" class=" btn btn-outline-primary bi bi-eye-fill"></a>
                    <a href="{{ path('app_game_edit', {'idMatch': game.idMatch}) }}" class=" btn btn-outline-primary bi bi-pencil-square"></a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="9">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_game_new') }}" class="btn btn-outline-primary">Create new</a>
{% endblock %}
